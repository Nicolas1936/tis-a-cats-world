<div class="container">
  <h1>'TIS A CAT'S WORLD</h1>
  <h2>Find your new best friend!</h2>

  <div class="cards">
      <% @cats.each do |cat| %>
        <%= render "cats/cat", cat: cat %>
      <% end %>
  </div>
</div>
